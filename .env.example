# SamaSalaire Backend — environment template.
# Copy to .env and fill in real values. Never commit .env.

# HTTP server
PORT=8080

# MongoDB
MONGODB_URI=mongodb://localhost:27017
DB_NAME=samasalaire

# JWT
JWT_SECRET=change-me
JWT_EXPIRY_HOURS=24
